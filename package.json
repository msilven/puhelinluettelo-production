{
  "name": "puhelinluettelon-backend",
  "version": "1.0.0",
  "description": "Puhelinluettelon backend ",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../palautusrepositorio/osa3/puhelinluettelo-frontend && npm run build && cp -r dist ../../../puhelinluettelo-production",
    "build:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Mikko Silvennoinen",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.2",
    "morgan": "^1.10.1"
  }
}
